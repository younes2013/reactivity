export interface Inscription {
  id: string;
  activityId: string;
  userId: string;
  registeredAt: string;
}

export interface MyInscription {
  inscriptionId: string;
  activityId: string;
  activityTitle: string;
  activityDate: string;
  registeredAt: string;
}
